services:
  app:
    build: .
    container_name: s-tier
    ports:
      - "127.0.0.1:3001:3000"
    environment:
      - GRID_API_KEY=${GRID_API_KEY}
      - PANDASCORE_API_KEY=${PANDASCORE_API_KEY}
    restart: unless-stopped
